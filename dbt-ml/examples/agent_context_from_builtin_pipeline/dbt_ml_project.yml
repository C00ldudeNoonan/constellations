name: agent_context_from_builtin_pipeline
version: "0.1.0"
profile: agent_context_from_builtin_pipeline

extraction:
  default_backend: json

source-paths: ["sources"]
model-paths: ["models"]
target-path: "target"
