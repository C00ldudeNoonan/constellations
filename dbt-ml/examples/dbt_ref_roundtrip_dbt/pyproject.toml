[project]
name = "dbt_ref_roundtrip_dbt"
version = "0.1.0"
description = "dbt-duckdb project proving the bidirectional dbt_ref round trip (#177)"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.9",
    # A real user's dbt project installs dbt-ml alongside dbt-duckdb in the same
    # venv so a dbt Python model can import it. Here it's a local editable path.
    "dbt-ml",
]

[tool.uv.sources]
dbt-ml = { path = "../..", editable = true }
