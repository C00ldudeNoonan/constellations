name: document_clustering
version: "0.1.0"
profile: document_clustering
extraction:
  default_backend: json
source-paths: ["sources"]
model-paths: ["models"]
transform-paths: ["transforms"]
target-path: "target"
