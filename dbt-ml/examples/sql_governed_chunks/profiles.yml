sql_governed_chunks:
  target: dev
  outputs:
    dev:
      warehouse:
        type: duckdb
        path: ./target/dbt_ml.duckdb
        schema: dbt_ml

    # MotherDuck is the same DuckDB adapter over the network (issue #186).
    # `dbt-ml run --target motherduck` with MOTHERDUCK_TOKEN set in the
    # environment. No credentials are committed here.
    motherduck:
      warehouse:
        type: duckdb
        path: md:sql_governed_chunks
        token: "{{ env_var('MOTHERDUCK_TOKEN') }}"
        schema: dbt_ml
