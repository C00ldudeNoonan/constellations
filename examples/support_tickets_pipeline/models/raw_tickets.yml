version: 2

models:
  - name: raw_tickets
    description: "Typed table of support tickets, one row per ticket"
    source: ref('support_tickets')

    extraction:
      backend: json
      options:
        fields:
          - ticket_id
          - product
          - priority
          - status
          - customer_tier
          - assigned_team
          - customer_name
          - customer_email
          - summary
          - created_at
          - first_response_at
          - resolved_at
          - sla_target_hours

    materialization: incremental

    fields:
      - name: ticket_id
        data_type: string
      - name: product
        data_type: string
      - name: priority
        data_type: string
      - name: status
        data_type: string
      - name: customer_tier
        data_type: string
      - name: assigned_team
        data_type: string
      - name: customer_name
        data_type: string
      - name: customer_email
        data_type: string
      - name: summary
        data_type: string
      - name: created_at
        data_type: string
      - name: first_response_at
        data_type: string
      - name: resolved_at
        data_type: string
      - name: sla_target_hours
        data_type: float

    tags: [raw, tickets]

    tests:
      - not_null: [ticket_id, priority, status, created_at]
      - unique: ticket_id
