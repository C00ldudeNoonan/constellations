version: 2

models:
  - name: raw_pdf_text
    description: "Extract text + page count from each PDF"
    source: ref('documents')

    extraction:
      backend: pdf
      options:
        include_text: true
        include_page_count: true

    materialization: incremental

    fields:
      - name: text
        data_type: string
      - name: page_count
        data_type: integer

    tags: [raw, pdf]

    tests:
      - not_null: [text]
      - unique: source_path
