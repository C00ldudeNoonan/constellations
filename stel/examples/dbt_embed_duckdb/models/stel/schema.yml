# GENERATED by `stel codegen` — do not edit; regenerate to update.
# stel project 'invoice_pipeline', model '<all>'.
version: 2
models:
- name: invoice_summary
  description: Per-vendor aggregation
  tags:
  - agg
  - invoices
  columns:
  - name: vendor
    tests:
    - not_null
  - name: invoice_count
    tests:
    - not_null
  - name: total_spend
    tests:
    - not_null
- name: monthly_totals
  description: Per-month invoice volume and spend
  tags:
  - agg
  - invoices
  - monthly
  columns:
  - name: month
    description: First-of-month timestamp (YYYY-MM-01)
    tests:
    - not_null
    - unique
  - name: invoice_count
    tests:
    - not_null
  - name: total_spend
    tests:
    - not_null
- name: raw_invoices
  description: Project synthetic invoice JSON into a typed table
  tags:
  - raw
  - invoices
  columns:
  - name: invoice_id
    data_type: string
    tests:
    - unique
  - name: vendor
    data_type: string
    tests:
    - not_null
  - name: issue_date
    data_type: string
  - name: line_items
    data_type: string
  - name: total
    data_type: float
    tests:
    - not_null
  - name: currency
    data_type: string
