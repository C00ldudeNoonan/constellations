# GENERATED by `stel codegen` — do not edit; regenerate to update.
# stel project 'dbt_ref_roundtrip', model '<all>'.
version: 2
models:
- name: flagged_invoices
  description: "Reads the dbt-built mart `invoice_facts` (itself built from the stel\
    \ extraction model `raw_vendors`) back into stel \u2014 the reverse (dbt ->\
    \ stel) half of the round trip: stel feeds dbt, and dbt feeds back into a\
    \ stel transform, all in one `dbt build` (#177).\n"
  tags:
  - dbt_ref
  - roundtrip
  columns:
  - name: vendor
    data_type: string
    tests:
    - not_null
    - unique
  - name: total_spend
    data_type: float
    tests:
    - not_null
  - name: flagged
    data_type: boolean
    tests:
    - not_null
- name: raw_vendors
  description: "Project synthetic vendor-spend documents into a typed table \u2014\
    \ the forward (stel -> dbt) half of the round trip (#177).\n"
  tags:
  - roundtrip
  columns:
  - name: vendor
    data_type: string
    tests:
    - not_null
    - unique
  - name: spend
    data_type: float
    tests:
    - not_null
